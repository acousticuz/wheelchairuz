# ============================================================
#  wheelchair.uz — Backend Environment Configuration
#  Bu faylni nusxa olib .env deb nomlang va to'ldiring:
#    cp .env.example .env
# ============================================================

# ── Server ───────────────────────────────────────────────────
PORT=3000
NODE_ENV=production

# ── Database (MySQL) ──────────────────────────────────────────
# MySQL 8.0.13+ yoki MariaDB 10.4.3+ tavsiya etiladi (JSON ustunlar uchun)
# Postgres ishlatmoqchi bo'lsangiz: DB_TYPE=postgres va DB_PORT=5432
DB_TYPE=mysql
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=wheelchair_user
DB_PASSWORD=YOUR_MYSQL_PASSWORD_HERE
DB_DATABASE=wheelchair_db

# TypeORM jadval yaratish:
#   - production'da bu OCHILMAYDI (xavfsizlik). Sxemani yaratish uchun
#     bir martagina NODE_ENV=development bilan DB_SYNC=true ishlating,
#     keyin DB_SYNC=false qiling va NODE_ENV=production'ga qayting.
DB_SYNC=false
DB_LOGGING=false

# Boshqariladigan/cloud MySQL SSL talab qilsa: DB_SSL=true
DB_SSL=false
# DB_SSL=true va kerak bo'lsa: DB_SSL_REJECT_UNAUTHORIZED=false

# Demo mahsulotlarni (SAYQAL katalogi, 20 dona) seed qilish:
# birinchi marta true qilib ishga tushiring, keyin false
SEED_PRODUCTS=false

# ── JWT Authentication ────────────────────────────────────────
# Uzun va murakkab tasodifiy satr kiriting (kamida 64 belgi)
JWT_SECRET=YOUR_SUPER_SECRET_JWT_KEY_CHANGE_THIS_IN_PRODUCTION_MIN_64_CHARS
JWT_EXPIRES_IN=7d

# ── Admin Credentials (birinchi ishga tushganda yaratiladi) ───
ADMIN_EMAIL=admin@wheelchair.uz
ADMIN_PASSWORD=CHANGE_ME_TO_A_STRONG_ADMIN_PASSWORD
ADMIN_NAME=Admin

# ── Telegram Bot ──────────────────────────────────────────────
# @BotFather dan olingan token
TELEGRAM_BOT_TOKEN=YOUR_TELEGRAM_BOT_TOKEN_HERE

# So'rovlar (inquiries) keladigan Telegram chat ID
# Botni guruhga qo'shib /start bosing, keyin chat ID ni oling
TELEGRAM_ADMIN_CHAT_ID=YOUR_TELEGRAM_CHAT_ID_HERE

# ── File Upload ───────────────────────────────────────────────
# Rasmlar saqlanadigan papka (server ichida)
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=5242880

# ── Frontend URL (CORS uchun) ─────────────────────────────────
FRONTEND_URL=https://wheelchair.uz
ADMIN_URL=https://admin.wheelchair.uz

# ── Swagger API Docs ──────────────────────────────────────────
# production da false qiling yoki umuman yozmang (production default: false)
SWAGGER_ENABLED=false
